-- Les tables clients, client_orders, client_tickets, ticket_messages
-- sont définies dans src/lib/schema-espace-client.sql
-- Exécuter CE FICHIER EN PREMIER, puis 003_cms.sql
--
-- Ce fichier est un rappel — il n'y a rien à exécuter ici.
SELECT 'Voir schema-espace-client.sql' AS info;
